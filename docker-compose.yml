services:
  # -----------------------------------------
  # Database Layer
  # -----------------------------------------
  svt-local-db:
    image: postgres:15-alpine
    container_name: svt-local-db
    restart: unless-stopped
    env_file: .env.example
    networks:
      - svt-local
    ports:
      - "5432:5432"
    volumes:
      - svt-local-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U svt_user -d svt_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  # -----------------------------------------
  # Caching Layer
  # -----------------------------------------
  svt-local-redis:
    image: redis:7-alpine
    container_name: svt-local-redis
    restart: unless-stopped
    networks:
      - svt-local
    ports:
      - "6379:6379"

  # -----------------------------------------
  # Django Backend (Smart Server)
  # -----------------------------------------
  svt-local-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: svt-local-backend
    env_file: .env.example
    networks:
      - svt-local
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    depends_on:
      svt-local-db:
        condition: service_healthy
      svt-local-redis:
        condition: service_started
    command: >
      sh -c "python manage.py migrate && 
             python manage.py load_islands && 
             python manage.py runserver 0.0.0.0:8000"

  # -----------------------------------------
  # React Frontend (Dumb Client)
  # -----------------------------------------
  svt-local-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: svt-local-frontend
    env_file: .env.example
    networks:
      - svt-local
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - svt-local-backend

volumes:
  svt-local-db-data:

networks:
  svt-local:
